# == Schema Information
#
# Table name: operator_secret_kinds
# Database name: org_principal
#
#  id :bigint           not null, primary key
#
_fixture:
  model_class: OperatorSecretKind

# Table name: staff_secret_kinds
# Database name: org_principal
#
#  id :bigint           not null, primary key
#
login:
  id: <%= OperatorSecretKind::LOGIN %>
