# == Schema Information
#
# Table name: visitor_telephone_statuses
# Database name: com_principal
#
#  id :bigint           not null, primary key
#
unverified:
  id: <%= VisitorTelephoneStatus::UNVERIFIED %>

verified:
  id: <%= VisitorTelephoneStatus::VERIFIED %>

suspended:
  id: <%= VisitorTelephoneStatus::SUSPENDED %>

deleted:
  id: <%= VisitorTelephoneStatus::DELETED %>

nothing:
  id: <%= VisitorTelephoneStatus::NOTHING %>

unverified_with_sign_up:
  id: <%= VisitorTelephoneStatus::UNVERIFIED_WITH_SIGN_UP %>

verified_with_sign_up:
  id: <%= VisitorTelephoneStatus::VERIFIED_WITH_SIGN_UP %>
