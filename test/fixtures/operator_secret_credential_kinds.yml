# == Schema Information
#
# Table name: operator_secret_credential_kinds
# Database name: org_principal
#
#  id :bigint           not null, primary key
#
_fixture:
  model_class: OperatorSecretCredentialKind

# Table name: staff_secret_credential_kinds
# Database name: org_principal
#
#  id :bigint           not null, primary key
#
login:
  id: <%= OperatorSecretCredentialKind::LOGIN %>
