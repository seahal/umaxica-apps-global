# == Schema Information
#
# Table name: visitor_email_statuses
# Database name: guest
#
#  id :bigint           not null, primary key
#
unverified:
  id: <%= VisitorEmailStatus::UNVERIFIED %>

verified:
  id: <%= VisitorEmailStatus::VERIFIED %>

suspended:
  id: <%= VisitorEmailStatus::SUSPENDED %>

deleted:
  id: <%= VisitorEmailStatus::DELETED %>

nothing:
  id: <%= VisitorEmailStatus::NOTHING %>

unverified_with_sign_up:
  id: <%= VisitorEmailStatus::UNVERIFIED_WITH_SIGN_UP %>

verified_with_sign_up:
  id: <%= VisitorEmailStatus::VERIFIED_WITH_SIGN_UP %>
