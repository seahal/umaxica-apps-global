# == Schema Information
#
# Table name: visitor_visibilities
# Database name: com_principal
#
#  id :bigint           not null, primary key
#
nobody:
  id: <%= VisitorVisibility::NOBODY %>

visitor:
  id: <%= VisitorVisibility::VISITOR %>

staff:
  id: <%= VisitorVisibility::STAFF %>

both:
  id: <%= VisitorVisibility::BOTH %>
