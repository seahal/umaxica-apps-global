# Minimal fixture for staff_statuses
# == Schema Information
#
# Table name: staff_statuses
# Database name: org_principal
#
#  id :bigint           not null, primary key
#
_fixture:
  model_class: OperatorIdentityStatus

# Table name: staff_statuses
# Database name: org_principal
#
#  id :bigint           not null, primary key
#
nothing:
  id: 2
active:
  id: 1
reserved:
  id: 3
