<% content_for :page_title, t("sign.app.registration.checkpoint.age_restricted", default: "This sign-up method cannot create an account.") %>

<h1><%= t("sign.app.registration.checkpoint.age_restricted", default: "This sign-up method cannot create an account.") %></h1>
<p><%= @sign_up_age_restricted_message %></p>
<p><%= t("sign.app.registration.checkpoint.age_restricted_retry", default: "You can return to sign up and try again later.") %></p>

<%= button_to t("actions.back", default: "Back"), @sign_up_age_restricted_restart_path, method: :get, form: { data: { turbo: false } } %>
