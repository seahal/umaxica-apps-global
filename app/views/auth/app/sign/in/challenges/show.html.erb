<% title t("sign.app.in.mfa.title") %>

<div>
  <div>
    <h1><%= t("sign.app.in.mfa.title") %></h1>
    <p><%= t("sign.app.in.mfa.description") %></p>
  </div>

  <div>
    <% if @can_use_totp %>
      <p>
        <%= link_to t("sign.app.in.mfa.methods.totp"), new_auth_app_sign_in_challenge_totp_path %>
      </p>
    <% end %>

    <% if @can_use_passkey %>
      <p>
        <%= link_to t("sign.app.in.mfa.methods.passkey"), new_auth_app_sign_in_challenge_passkey_path %>
      </p>
    <% end %>

    <% unless @can_use_totp || @can_use_passkey %>
      <p><%= t("sign.app.in.mfa.no_methods_available") %></p>
      <p><%= link_to t("sign.app.authentication.new.back"), auth_app_sign_in_path %></p>
    <% end %>
  </div>
</div>
