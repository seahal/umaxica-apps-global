<h1><%= title "Dashboard" %></h1>

<p>Sign app signed-in landing.</p>

<section>
  <h2>Primary links</h2>
  <ul>
    <li><%= link_to "Root", auth_app_root_path(ri: params[:ri]) %></li>
    <li><%= link_to "Sign in", auth_app_sign_in_path(ri: params[:ri]) %></li>
    <li><%= link_to "Sign up", auth_app_sign_up_path(ri: params[:ri]) %></li>
    <li><%= link_to "Settings", auth_app_settings_path(ri: params[:ri]) %></li>
    <li><%= link_to "Logout", new_auth_app_sign_out_path(ri: params[:ri]) %></li>
  </ul>
</section>

<section>
  <h2>Ceremony links</h2>
  <ul>
    <li><%= link_to "Sign-in guard", auth_app_sign_in_guard_path(ri: params[:ri]) %></li>
    <li><%= link_to "Sign-in check", auth_app_sign_in_check_path(ri: params[:ri]) %></li>
    <li><%= link_to "Sign-in challenge", auth_app_sign_in_challenge_path(ri: params[:ri]) %></li>
    <li><%= link_to "Sign-in TOTP", new_auth_app_sign_in_challenge_totp_path(ri: params[:ri]) %></li>
    <li><%= link_to "Verification", auth_app_verification_path(ri: params[:ri]) %></li>
    <li><%= link_to "Verification TOTP", new_auth_app_verification_totp_path(ri: params[:ri]) %></li>
    <li>Selector: handled by the sign-in guard sequence, no direct dashboard route</li>
  </ul>
</section>
