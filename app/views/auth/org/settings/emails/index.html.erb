<% content_for :page_title, t("controller.sign.app.setting.index.email") %>
<%= link_to t("sign.org.settings.show.back"), auth_org_settings_path %>

<div>
  <%= link_to t("sign.org.settings.email.index.new_link"),
              new_auth_org_settings_emails_registration_path %>
</div>

<table>
  <thead>
    <tr>
      <th scope="col"><%= t("activerecord.attributes.staff_email.address") %></th>
      <th scope="col"><%= t("activerecord.attributes.staff_email.status") %></th>
      <th scope="col">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @staff_emails.each do |email| %>
      <tr>
        <td><%= email.address %></td>
        <td>
          <% if [OperatorEmailStatus::ACTIVE, OperatorEmailStatus::VERIFIED].include?(email.staff_email_status_id) %>
            <span><%= t("views.sign.org.settings.emails.index.verified") %></span>
          <% else %>
            <span><%= t("views.sign.org.settings.emails.index.unverified") %></span>
          <% end %>
        </td>
        <td>
          <%= link_to t("sign.org.settings.email.index.edit"),
                      edit_auth_org_settings_email_path(email.public_id) %>
        </td>
      </tr>
    <% end %>
    <% if @staff_emails.empty? %>
      <tr>
        <td colspan="3"><p><%= t("sign.org.settings.email.index.empty") %></p></td>
      </tr>
    <% end %>
  </tbody>
</table>
