<% title t("sign.recovery_passcodes.required.title") if respond_to?(:title) %>

<h1><%= t("sign.recovery_passcodes.required.title") %></h1>
<p>
  <%= t(
        "sign.recovery_passcodes.required.description",
        count: @usable_unused_recovery_passcode_count,
        minimum: @minimum_recovery_passcodes,
      ) %>
</p>
<p>
  <%= link_to t("sign.recovery_passcodes.required.setup_link"), @recovery_passcode_setup_url %>
</p>
