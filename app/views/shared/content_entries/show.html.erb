<% page_title @content_entry.title %>

<main>
  <article>
    <h1><%= @content_entry.title %></h1>
    <% if @content_entry.summary.present? %>
      <p><%= @content_entry.summary %></p>
    <% end %>
    <div><%= simple_format(@content_entry.body) %></div>
  </article>
</main>
