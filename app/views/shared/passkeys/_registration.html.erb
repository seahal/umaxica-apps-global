<div>
  <div
    data-controller="passkey-registration"
    data-passkey-registration-begin-url-value="<%= begin_url %>"
    data-passkey-registration-finish-url-value="<%= finish_url %>"
    data-passkey-registration-success-redirect-url-value="<%= success_redirect_url %>"
    <% if local_assigns[:checkpoint_version].present? %>
      data-passkey-registration-checkpoint-version-value="<%= checkpoint_version %>"
    <% end %>>

    <div>
      <label for="description">
        <%= t("sign.app.settings.passkeys.new.description_label") %>
      </label>
      <input type="text"
             id="description"
             autocomplete="off"
             data-passkey-registration-target="description"
             placeholder="<%= t("sign.app.settings.passkeys.new.description_placeholder") %>"
             maxlength="100">
    </div>

    <p data-passkey-registration-target="error" class="hidden"></p>
    <p data-passkey-registration-target="status" class="hidden"></p>

    <div>
      <button type="button"
              data-action="click->passkey-registration#register">
        <%= t("sign.app.settings.passkeys.new.submit") %>
      </button>
    </div>
  </div>
</div>
