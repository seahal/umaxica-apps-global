<!DOCTYPE html>
<html>
  <head>
    <title>Inertia Rails Example</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <% unless Rails.env.test? %>
      <%= vite_client_tag %>
      <%= vite_javascript_tag 'application' %>
    <% end %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
