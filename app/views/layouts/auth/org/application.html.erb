<% title_site = "#{ENV.fetch("BRAND_NAME")} (org) Auth" %>
<!DOCTYPE html>
<html lang="<%= get_language %>" data-theme="<%= theme_cookie_value %>" class="<%= theme_html_class %>">
<head>
  <meta charset="utf-8">
  <%= display_meta_tags site: title_site, title: page_title %>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="turbo-refresh-method" content="morph">
  <meta name="turbo-refresh-scroll" content="preserve">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= vite_client_tag nonce: true %>
  <%= vite_react_refresh_tag nonce: true %>
  <%= vite_typescript_tag "entrypoints/sign/org", nonce: true, "data-turbo-eval": "false" %>
  <%= inertia_ssr_head %>
  <%= render "layouts/shared/cloudflare_turnstile_api" %>
</head>
<body>
  <header>
    <%= render "layouts/shared/flash_messages" %>
    <%= render "layouts/shared/current_banner", tld: :org, domain: :sign, region: :global %>
    <% unless @hide_auth_navigation %>
      <nav aria-label="Primary">
        <ul>
          <% if logged_in? %>
            <li><%= link_to t("sign.org.layout.nav.logout", default: "Log Out"), new_auth_org_sign_out_path %></li>
          <% else %>
            <li><%= link_to t("sign.org.layout.nav.sign_up"), auth_org_sign_up_url %></li>
            <li><%= link_to t("sign.org.layout.nav.log_in"), auth_org_sign_in_url %></li>
          <% end %>
        </ul>
      </nav>
    <% end %>
    <p><strong><%= link_to ENV.fetch("BRAND_NAME"), auth_org_root_path %></strong> (org)</p>
  </header>

  <main id="main">
    <%= yield %>
  </main>

  <footer>
    <nav aria-label="Footer">
      <ul>
        <% if logged_in? %>
          <li><%= link_to t("sign.org.preferences.footer.dashboard"), base_org_dashboard_url(ri: current_region_identifier, host: base_authority_host) %></li>
        <% else %>
          <li><%= link_to t("sign.org.preferences.footer.home"), auth_org_root_url %></li>
        <% end %>
        <li><%= link_to t("sign.org.preferences.footer.preference"), auth_org_settings_path %></li>
        <li><%= link_to t("sign.org.preferences.footer.settings"), auth_org_settings_path %></li>
      </ul>
    </nav>
    <aside aria-label="Preferences">
      <%= render "layouts/shared/footer_cookie_controls", scope: :org, base_host: base_authority_host %>
      <%= render "layouts/shared/footer_theme_controls", scope: :org %>
    </aside>
    <div>
      <%= render "layouts/shared/copyright" %>
    </div>
  </footer>
</body>
</html>
