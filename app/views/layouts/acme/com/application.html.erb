<!DOCTYPE html>
<html lang="<%= get_language %>" data-theme="<%= theme_cookie_value %>" class="<%= theme_html_class %>">
<head>
  <%= display_meta_tags site: "#{ENV.fetch('BRAND_NAME', 'UMAXICA').upcase} (com)", title: page_title %>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="turbo-refresh-method" content="morph">
  <meta name="turbo-refresh-scroll" content="preserve">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= vite_client_tag %>
  <%= vite_typescript_tag "application", nonce: true, "data-turbo-eval": "false" %>
  <%= inertia_ssr_head %>
  <%= yield :head %>
</head>

<body>

<header>
  <nav>
    <%= render "layouts/shared/flash_messages" %>
    <%= render "layouts/shared/current_banner", tld: :com, domain: :acme, region: :global %>
    <% unless logged_in? %>
      <%= link_to "#{t('sign.app.layout.nav.sign_up')} / #{t('sign.app.layout.nav.log_in')}", acme_com_oidc_authorization_path %>
    <% else %>
      <%= button_to t("sign.app.layout.nav.logout"), acme_com_oidc_logout_path, method: :post %>
    <% end %>
  </nav>
  <div>
    <span>ACME</span>
    <h1>
      <a href="<%= "#{request.protocol}#{ENV.fetch('ACME_CORPORATE_URL', 'com.localhost')}#{request.port_string}/" %>"><%= ENV.fetch("BRAND_NAME", "Umaxica") %></a>
      <span>(com)</span>
    </h1>
  </div>
</header>

<main>
  <%= yield %>
</main>

<footer>
  <nav>
    <ul>
      <li><a href="<%= "#{request.protocol}#{ENV.fetch('ACME_CORPORATE_URL', 'com.localhost')}#{request.port_string}/" %>"><%= t('acme.com.preferences.footer.home') %></a></li>
      <li><%= link_to t('acme.com.preferences.footer.preference'), acme_com_preference_url(host: ENV.fetch('ACME_CORPORATE_URL', 'com.localhost')) %></li>
    </ul>
  </nav>
  <%= render "layouts/shared/footer_cookie_controls", scope: :com %>
  <%= render "layouts/shared/footer_theme_controls", scope: :com %>
  <div>
    <%= render "layouts/shared/copyright" %>
  </div>
</footer>
</body>
</html>
