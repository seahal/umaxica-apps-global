<!DOCTYPE html>
<html lang="<%= get_language %>" data-theme="<%= theme_cookie_value %>" class="<%= theme_html_class %>">
<head>
  <%= display_meta_tags site: "#{ENV.fetch('BRAND_NAME', 'UMAXICA').upcase} (org)", title: page_title %>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="turbo-refresh-method" content="morph">
  <meta name="turbo-refresh-scroll" content="preserve">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= vite_client_tag %>
  <%= vite_react_refresh_tag %>
  <%= vite_typescript_tag "acme/org", nonce: true, "data-turbo-eval": "false" %>
  <%= inertia_ssr_head %>
  <%= yield :head %>
</head>

<body>

<header>
  <nav>
    <%= render "layouts/shared/flash_messages" %>
    <%= render "layouts/shared/current_banner", tld: :org, domain: :acme, region: :global %>
    <% unless logged_in? %>
      <%= link_to "#{t('sign.org.layout.nav.sign_up')} / #{t('sign.org.layout.nav.log_in')}", acme_org_oidc_authorization_path %>
    <% else %>
      <%= link_to t("sign.org.layout.nav.logout"), new_acme_org_sign_out_path(ri: params[:ri]) %>
    <% end %>
  </nav>
  <div>
    <span>ACME</span>
    <h1>
      <a href="<%= "#{request.protocol}#{ENV.fetch('ACME_STAFF_URL', 'org.localhost')}#{request.port_string}/" %>"><%= ENV.fetch("BRAND_NAME", "Umaxica") %></a>
      <span>(org)</span>
    </h1>
  </div>
</header>

<main>
  <%= yield %>
</main>

<footer>
  <nav>
    <ul>
      <li><a href="<%= "#{request.protocol}#{ENV.fetch('ACME_STAFF_URL', 'org.localhost')}#{request.port_string}/" %>"><%= t('acme.org.preferences.footer.home') %></a></li>
      <li><%= link_to t('acme.org.preferences.footer.preference'), acme_org_preference_url(host: ENV.fetch('ACME_STAFF_URL', 'org.localhost')) %></li>
    </ul>
  </nav>
  <%= render "layouts/shared/footer_cookie_controls", scope: :org %>
  <%= render "layouts/shared/footer_theme_controls", scope: :org %>
  <div>
    <%= render "layouts/shared/copyright" %>
  </div>
</footer>
</body>
</html>
