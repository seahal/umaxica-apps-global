<% content_for :page_title, t("sign.org.settings.secret_credentials.edit.title", default: "Edit secret") %>

<h3><%= t("sign.org.settings.secret_credentials.edit.title", default: "Edit secret") %></h3>

<%= form_with(model: @secret_credential, url: acme_org_settings_secret_path(@secret_credential, ri: params[:ri]), method: :patch) do |form| %>
  <% if @secret_credential.errors.any? %>
    <ul>
      <% @secret_credential.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>

  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= render "shared/cloudflare_turnstile_stealth" %>

  <%= link_to t("actions.cancel"), acme_org_settings_secrets_path(ri: params[:ri]) %>
  <%= form.submit t("actions.update") %>
<% end %>
