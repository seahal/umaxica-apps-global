<% surface = controller_path.split("/").second %>
<% content_for :page_title, t("acme.#{surface}.preferences.title") %>

<section class="animate-fade-in-up">
  <div>
    <%= link_to public_send("acme_#{surface}_root_path", ct: "dr", lx: "en", ri: "us", tz: "asia/tokyo") do %>
      <span><%= t("acme.#{surface}.preferences.up_link") %></span>
    <% end %>
  </div>

  <div>
    <div>
      <h1><%= t("acme.#{surface}.preferences.title") %></h1>
      <p><%= t("acme.#{surface}.preferences.description") %></p>
    </div>

    <div>
      <% {
           region: "region_settings",
           timezone: "timezone_settings",
           language: "language_settings",
           motion: "motion_settings",
           density: "density_settings",
           page_size: "page_size_settings",
           adult_content_gate: "adult_content_gate_settings",
           theme: "theme_settings",
           cookie: "cookie_settings",
           reset: "reset_settings",
         }.each do |screen, label_key| %>
        <%= link_to public_send("edit_acme_#{surface}_preference_#{screen}_path") do %>
          <div>
            <span><%= t("acme.#{surface}.preferences.#{label_key}") %></span>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</section>
