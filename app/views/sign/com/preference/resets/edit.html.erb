<% content_for :page_title, t('acme.com.preference.resets.title') %>

<section>
  <div>
    <%= link_to t("acme.com.preferences.regions.back_link"),
                sign_com_preference_path(lx: params[:lx], ri: params[:ri]) %>
  </div>

  <h1>
    <%= t("acme.com.preference.resets.title") %>
  </h1>

  <p>
    <%= t("acme.com.preference.resets.description") %>
  </p>

  <%= form_with url: sign_com_preference_reset_path, method: :delete do |f| %>
    <% if @preference&.errors&.any? %>
      <div>
        <ul>
          <% @preference.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= check_box_tag :confirm_reset, "1", @preference&.confirm_reset == "1", required: true %>
      <%= label_tag :confirm_reset, t("acme.com.preference.resets.confirm") %>
    </div>

    <div>
      <%= f.submit t("acme.com.preference.resets.button"),
                   data: {
                     turbo_submits_with: t("acme.com.preferences.submitting") } %>
    </div>
  <% end %>
</section>
