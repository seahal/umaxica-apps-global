<% content_for :page_title, t("acme.com.preferences.theme_settings") %>

<section>
  <div>
    <%= link_to t("actions.back"), sign_com_preference_url %>
  </div>

  <h1>
    <%= t("acme.com.preference.theme.edit.title") %>
  </h1>
  <p>
    <%= t("acme.com.preference.theme.edit.description") %>
  </p>

  <%= form_with model: @preference_theme, scope: :preference_theme, url: sign_com_preference_theme_path, method: :patch do |f| %>
    <div>
      <%= f.label :option_id, t("acme.com.preferences.theme.select_theme") %>
      <div>
        <%= f.select :option_id,
                     options_for_select(
                       [
                         [t("acme.com.preference.theme.edit.options.light"), "light"],
                         [t("acme.com.preference.theme.edit.options.dark"), "dark"],
                         [t("acme.com.preference.theme.edit.options.system"), "system"]
                       ],
                       @preference_theme.option&.name || "system"),
                     {} %>
      </div>
    </div>

    <div>
      <%= f.submit t("acme.com.preferences.update_settings"),
                   data: { turbo_submits_with: t("acme.com.preferences.submitting") } %>
    </div>
  <% end %>
</section>
