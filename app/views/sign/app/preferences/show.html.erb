<section class="animate-fade-in-up">
  <div>
    <%= link_to sign_app_root_path(ct: "dr", lx: "en", ri: "us", tz: "jst") do %>
      <span><%= t("apex.app.preferences.up_link") %></span>
    <% end %>
  </div>

  <div>
    <div>
      <div>
        <div>
          <h1>
            <%= title t("apex.app.preferences.title") %>
          </h1>
          <p>
            <%= t("apex.app.preferences.description") %>
          </p>
        </div>
      </div>
    </div>

    <div>
      <!-- Region -->
      <%= link_to edit_sign_app_preference_region_url do %>
        <div>
          <span><%= t("apex.app.preferences.region_settings") %></span>
        </div>
      <% end %>

      <!-- Timezone -->
      <%= link_to edit_sign_app_preference_region_timezone_url do %>
        <div>
          <span><%= t("apex.app.preferences.timezone_settings") %></span>
        </div>
      <% end %>

      <!-- Language -->
      <%= link_to edit_sign_app_preference_region_language_url do %>
        <div>
          <span><%= t("apex.app.preferences.language_settings") %></span>
        </div>
      <% end %>

      <%= link_to edit_sign_app_preference_accessibility_motion_url do %>
        <div>
          <span><%= t("apex.app.preferences.motion_settings") %></span>
        </div>
      <% end %>

      <%= link_to edit_sign_app_preference_display_density_url do %>
        <div>
          <span><%= t("apex.app.preferences.density_settings") %></span>
        </div>
      <% end %>

      <%= link_to edit_sign_app_preference_display_items_per_page_url do %>
        <div>
          <span><%= t("apex.app.preferences.items_per_page_settings") %></span>
        </div>
      <% end %>

      <!-- Theme -->
      <%= link_to edit_sign_app_preference_theme_url do %>
        <div>
          <span><%= t("apex.app.preferences.theme_settings") %></span>
        </div>
      <% end %>

      <!-- Cookie -->
      <%= link_to edit_sign_app_preference_cookie_url do %>
        <div>
          <span><%= t("apex.app.preferences.cookie_settings") %></span>
        </div>
      <% end %>

      <!-- Reset -->
      <%= link_to edit_sign_app_preference_reset_url do %>
        <div>
          <span><%= t("apex.app.preferences.reset_settings") %></span>
        </div>
      <% end %>

    </div>
  </div>
</section>
