<section class="animate-fade-in-up">
  <div>
    <%= link_to sign_app_root_path(ct: "dr", lx: "en", ri: "us", tz: "asia/tokyo") do %>
      <span><%= t("acme.app.preferences.up_link") %></span>
    <% end %>
  </div>

  <div>
    <div>
      <div>
        <div>
          <h1>
            <%= title t("acme.app.preferences.title") %>
          </h1>
          <p>
            <%= t("acme.app.preferences.description") %>
          </p>
        </div>
      </div>
    </div>

    <div>
      <!-- Region -->
      <%= link_to edit_sign_app_preference_region_url do %>
        <div>
          <span><%= t("acme.app.preferences.region_settings") %></span>
        </div>
      <% end %>

      <!-- Timezone -->
      <%= link_to edit_sign_app_preference_timezone_url do %>
        <div>
          <span><%= t("acme.app.preferences.timezone_settings") %></span>
        </div>
      <% end %>

      <!-- Language -->
      <%= link_to edit_sign_app_preference_language_url do %>
        <div>
          <span><%= t("acme.app.preferences.language_settings") %></span>
        </div>
      <% end %>

      <%= link_to edit_sign_app_preference_motion_url do %>
        <div>
          <span><%= t("acme.app.preferences.motion_settings") %></span>
        </div>
      <% end %>

      <%= link_to edit_sign_app_preference_density_url do %>
        <div>
          <span><%= t("acme.app.preferences.density_settings") %></span>
        </div>
      <% end %>

      <%= link_to edit_sign_app_preference_page_size_url do %>
        <div>
          <span><%= t("acme.app.preferences.page_size_settings") %></span>
        </div>
      <% end %>

      <%= link_to edit_sign_app_preference_adult_content_gate_url do %>
        <div>
          <span><%= t("acme.app.preferences.adult_content_gate_settings") %></span>
        </div>
      <% end %>

      <!-- Theme -->
      <%= link_to edit_sign_app_preference_theme_url do %>
        <div>
          <span><%= t("acme.app.preferences.theme_settings") %></span>
        </div>
      <% end %>

      <!-- Cookie -->
      <%= link_to edit_sign_app_preference_cookie_url do %>
        <div>
          <span><%= t("acme.app.preferences.cookie_settings") %></span>
        </div>
      <% end %>

      <!-- Reset -->
      <%= link_to edit_sign_app_preference_reset_url do %>
        <div>
          <span><%= t("acme.app.preferences.reset_settings") %></span>
        </div>
      <% end %>

    </div>
  </div>
</section>
