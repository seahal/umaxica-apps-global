<% content_for :page_title, t("apex.app.preferences.language_settings") %>

<section>
  <h1>
      <%= t("apex.app.preference.language.edit.heading") %>
  </h1>
  <p>
      <%= t("apex.app.preference.language.edit.description") %>
  </p>
  <%= form_with model: @preference_language, scope: :preference_language, url: sign_app_preference_region_language_path, method: :patch do |f| %>
    <div>
      <%= f.label :option_id,
                  t("apex.app.preference.language.edit.language_label") %>
      <div>
        <%= f.select :option_id,
                     options_for_select(
                       AppPreferenceLanguageOption.order(:id).map do |option|
                         [
                           case option.name
                           when "ja" then t("languages.japanese")
                           when "en" then t("languages.english")
                           else option.name
                           end,
                           option.id,
                         ]
                       end,
                       @preference_language.option_id,
                     ),
                     { include_blank: false },
                     {} %>
      </div>
    </div>

    <div>
      <%= f.submit t("apex.app.preferences.update_settings"),
                   data: { turbo_submits_with: t("apex.app.preferences.submitting") } %>
      <%= link_to t("apex.app.preferences.region_settings"), edit_sign_app_preference_region_path %>
      <%= link_to t("apex.app.preferences.back_to_settings"), sign_app_preference_url %>
    </div>
  <% end %>
</section>
