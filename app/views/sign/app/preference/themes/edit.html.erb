<% content_for :page_title, t("apex.app.preferences.theme_settings") %>

<section>
  <h1>
    <%= t("apex.app.preference.theme.edit.title") %>
  </h1>
  <p>
    <%= t("apex.app.preference.theme.edit.description") %>
  </p>

  <%= form_with model: @preference_theme, scope: :preference_theme, url: sign_app_preference_theme_path, method: :patch do |f| %>
    <div>
      <%= f.label :option_id, t("apex.app.preferences.theme.select_theme") %>
      <div>
        <%= f.select :option_id,
                     options_for_select(
                       [
                         [t("apex.app.preference.theme.edit.options.light"), "light"],
                         [t("apex.app.preference.theme.edit.options.dark"), "dark"],
                         [t("apex.app.preference.theme.edit.options.system"), "system"]
                       ],
                       @preference_theme.option&.name || "system"),
                     {} %>
      </div>
    </div>

    <div>
      <%= f.submit t("apex.app.preferences.update_settings"),
                   data: { turbo_submits_with: t("apex.app.preferences.submitting") } %>
      <%= link_to t("apex.app.preferences.back_to_settings"), sign_app_preference_url %>
    </div>
  <% end %>
</section>
