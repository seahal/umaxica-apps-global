<% content_for :page_title, t("sign.app.configuration.passkeys.edit.title") %>
<%= link_to t("sign.app.configuration.show.back"), sign_app_configuration_passkeys_path(ri: params[:ri]) %>

<div>
  <div>
    <h1><%= t("sign.app.configuration.passkeys.edit.title") %></h1>
    <p><%= t("sign.app.configuration.passkeys.edit.description") %></p>
  </div>

  <%= form_with model: @passkey, url: sign_app_configuration_passkey_path(@passkey), method: :patch do |form| %>
    <% if @passkey.errors.any? %>
      <div>
        <div>
          <div>
            <h3>
              <%= t('errors.messages.validation_errors', count: @passkey.errors.count) %>
            </h3>
            <div>
              <ul>
                <% @passkey.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <div>
      <%= form.label :description, t('activerecord.attributes.user_passkey.description') %>
      <%= form.text_field :description,
                          maxlength: 100 %>
    </div>

    <%= render "shared/cloudflare_turnstile_stealth" %>

    <div>
      <%= form.submit t('actions.save') %>
      <%= link_to t('sign.common.cancel'), sign_app_configuration_passkeys_path %>
    </div>
  <% end %>

  <div>
    <%= form_with url: sign_app_configuration_passkey_path(@passkey),
                  method: :delete,
                  data: { turbo_confirm: t('messages.confirm_destroy') } do |form| %>
      <%= render "shared/cloudflare_turnstile_stealth" %>
      <%= form.submit t('actions.delete') %>
    <% end %>
  </div>
</div>
