<%= link_to t("sign.app.settings.show.back"), sign_app_settings_passkeys_path(ri: params[:ri]) %>

<% title t("sign.app.settings.passkeys.new.page_title") %>

<div>
  <div>
    <h1><%= t("sign.app.settings.passkeys.new.page_title") %></h1>
    <p><%= t("sign.app.settings.passkeys.new.description") %></p>
  </div>

  <div
    data-controller="passkey-registration"
    data-passkey-registration-options-url-value="<%= sign_app_settings_passkeys_options_path %>"
    data-passkey-registration-verification-url-value="<%= sign_app_settings_passkeys_verification_path %>"
    data-passkey-registration-turnstile-site-key-value="<%= JitSecurityTurnstileConfig.stealth_site_key.to_s %>"
    data-passkey-registration-turnstile-error-message-value="<%= t("turnstile_error") %>">

    <div>
      <label for="description">
        <%= t("sign.app.settings.passkeys.new.description_label") %>
      </label>
      <input type="text"
             id="description"
             autocomplete="off"
             data-passkey-registration-target="description"
             placeholder="<%= t("sign.app.settings.passkeys.new.description_placeholder") %>"
             maxlength="100">
    </div>

    <p data-passkey-registration-target="error" class="hidden"></p>
    <p data-passkey-registration-target="status" class="hidden"></p>
    <input type="hidden" data-passkey-registration-target="turnstileResponse" autocomplete="off">

    <div>
      <button type="button"
              data-action="click->passkey-registration#register">
        <%= t("sign.app.settings.passkeys.new.submit") %>
      </button>
      <%= link_to t("sign.common.cancel"), sign_app_settings_passkeys_path %>
    </div>
  </div>
</div>
