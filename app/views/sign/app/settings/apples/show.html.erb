<% content_for :page_title, t("controller.sign.app.setting.index.apple") %>

<%= link_to t("sign.app.settings.show.back"), sign_app_settings_path %>

<div>
  <h3><%= t("controller.sign.app.setting.index.apple") %></h3>
  <p><%= t("views.sign.app.settings.apples.show.description") %></p>
  <p>
    <% if current_client.active_social_provider?("apple") %>
      <%= t("views.sign.app.settings.apples.show.linked") %>
    <% else %>
      <%= t("views.sign.app.settings.apples.show.unlinked") %>
    <% end %>
  </p>
  <%= link_to t("actions.edit"), edit_sign_app_settings_apple_path(ri: params[:ri]) %>
</div>
