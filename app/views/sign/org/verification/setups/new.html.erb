<% content_for :page_title, t("sign.org.verification.setup.title") %>
<% title t("sign.org.verification.setup.title") %>

<div>
  <h1><%= t("sign.org.verification.setup.title") %></h1>
  <p><%= t("sign.org.verification.setup.description") %></p>

  <% if @return_to.present? %>
    <p>
      <%= link_to t("actions.back"), @return_to %>
    </p>
  <% end %>

  <ul>
    <% if @missing_methods.include?(:passkey) %>
    <li>
      <%= link_to t("sign.org.verification.setup.methods.passkey"),
                  new_sign_org_configuration_passkey_path(ri: params[:ri], rt: @rt) %>
    </li>
    <% end %>
  </ul>
</div>
