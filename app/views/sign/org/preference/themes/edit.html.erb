<% content_for :page_title, t("acme.org.preferences.theme_settings") %>

<section>
  <div>
    <%= link_to t("actions.back"), sign_org_preference_url %>
  </div>

  <h1>
    <%= t("acme.org.preference.theme.edit.title") %>
  </h1>
  <p>
    <%= t("acme.org.preference.theme.edit.description") %>
  </p>

  <%= form_with model: @preference_theme, scope: :preference_theme, url: sign_org_preference_theme_path, method: :patch do |f| %>
    <div>
      <%= f.label :option_id, t("acme.org.preferences.theme.select_theme") %>
      <div>
        <%= f.select :option_id,
                     options_for_select(
                       [
                         [t("acme.org.preference.theme.edit.options.light"), "light"],
                         [t("acme.org.preference.theme.edit.options.dark"), "dark"],
                         [t("acme.org.preference.theme.edit.options.system"), "system"]
                       ],
                       @preference_theme.option&.name || "system"),
                     {} %>
      </div>
    </div>

    <div>
      <%= f.submit t("acme.org.preferences.update_settings"),
                   data: { turbo_submits_with: t("acme.org.preferences.submitting") } %>
    </div>
  <% end %>
</section>
