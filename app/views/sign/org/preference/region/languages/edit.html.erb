<% content_for :page_title, 'Edit Language' %>
<section>
  <div>
    <%= link_to t("acme.org.preferences.regions.back_link"),
                edit_sign_org_preference_region_path(lx: params[:lx], ri: params[:ri]) %>
  </div>

  <h1>
      <%= t("acme.org.preference.language.edit.heading") %>
  </h1>
  <p>
      <%= t("acme.org.preference.language.edit.description") %>
  </p>

  <%= form_with model: @preference_language, scope: :preference_language, url: sign_org_preference_region_language_path, method: :patch do |f| %>
    <div>
      <%= f.label :option_id,
                  t("acme.org.preference.language.edit.language_label") %>
      <div>
        <%= f.select :option_id,
                     options_for_select(
                       preference_language_options(OrgPreferenceLanguageOption),
                       preference_language_selected(@preference_language.option_id, OrgPreferenceLanguageOption),
                     ),
                     { include_blank: false },
                     {} %>
      </div>
    </div>

    <div>
      <%= f.submit t("acme.org.preferences.update_settings"),
                   data: { turbo_submits_with: t("acme.org.preferences.submitting") } %>
      <%= link_to t("acme.org.preferences.region_settings"), edit_sign_org_preference_region_path %>
    </div>
  <% end %>
</section>
