<% content_for :page_title, t("controller.sign.app.setting.index.email") %>
<%= link_to t("sign.org.configuration.show.back"), sign_org_configuration_path %>

<div>
  <%= link_to t("sign.org.configuration.email.index.new_link"),
              new_sign_org_configuration_emails_registration_path %>
</div>

<table>
  <thead>
    <tr>
      <th scope="col"><%= t("activerecord.attributes.staff_email.address") %></th>
      <th scope="col"><%= t("activerecord.attributes.staff_email.status") %></th>
      <th scope="col">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @staff_emails.each do |email| %>
      <tr>
        <td><%= email.address %></td>
        <td>
          <% if [OperatorEmailStatus::ACTIVE, OperatorEmailStatus::VERIFIED].include?(email.staff_email_status_id) %>
            <span><%= t("views.sign.org.configuration.emails.index.verified") %></span>
          <% else %>
            <span><%= t("views.sign.org.configuration.emails.index.unverified") %></span>
          <% end %>
        </td>
        <td>
          <%= link_to t("sign.org.configuration.email.index.edit"),
                      edit_sign_org_configuration_email_path(email.public_id) %>
        </td>
      </tr>
    <% end %>
    <% if @staff_emails.empty? %>
      <tr>
        <td colspan="3"><p><%= t("sign.org.configuration.email.index.empty") %></p></td>
      </tr>
    <% end %>
  </tbody>
</table>
