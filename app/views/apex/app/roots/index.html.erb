<% content_for :page_title, "#{ENV['BRAND_NAME']} (app)" %>

<section>
  <h1>
    Welcome to <%= ENV["BRAND_NAME"] %> (app)
  </h1>
  <p>
    <%= t("views.apex.app.roots.index.redirect_prompt", preference: t("apex.app.preferences.footer.preference")) %>
  </p>

  <ul>
    <li>
      <a
        href="https://<%= ENV['EDGE_SERVICE_URL'] %>"
        data-turbo-frame="_top">
        <%= t("apex.app.preferences.footer.home") %>
      </a>
    </li>
    <li>
      <%= link_to t("apex.app.configurations.title"), apex_app_configuration_path,
                  data: { turbo_frame: "_top" } %>
    </li>
    <li>
      <%= link_to t("apex.app.preferences.footer.preference"), sign_app_preference_path,
                  data: { turbo_frame: "_top" } %>
    </li>
  </ul>
</section>
