// base/app resolves pages only from src/pages/base/app, so each surface needs its own module for
// the shared preference index component.
export { default } from "@/features/preferences/PreferenceIndex";
