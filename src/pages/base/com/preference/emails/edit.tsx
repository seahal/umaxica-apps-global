// base/com resolves pages only from src/pages/base/com, so each surface needs its own module for the
// shared unsubscribe screen.
export { default } from "@/features/preferences/PreferenceEmailUnsubscribe";
