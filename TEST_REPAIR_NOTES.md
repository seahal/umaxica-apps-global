# Test Repair Notes

## Batch 3 (this session)

- Fixed `test/integration/core_rp_browser_flow_test.rb`: updated `/auth` → `/oidc/authorization`,
  `/auth/callback` → `/oidc/callback` per actual routes; added `to:` key to `assert_routing`
  expectations (route has explicit `to:` option); fixed JWT issuer/key mismatch for org/com surfaces
  by passing resource-type-based `jwt_issuer_id`/`issuer` to `OidcIdTokenIssuer.call`; added
  `oidc_resource_type_for` helper; restructured logout tests to set up auth first and assert correct
  Acme OIDC logout redirect (not local root).
- Fixed `test/integration/oidc_rp_browser_flow_test.rb`: same `/auth` → `/oidc/authorization` and
  `/auth/callback` → `/oidc/callback` path fixes; fixed hardcoded `"www.app.localhost"` host to use
  `ENV.fetch("ACME_SERVICE_URL", ...)` (route constrained to configured host); added
  `oidc_resource_type_for` helper and resource-type-specific JWT issuer for org/com surfaces;
  updated nonce-mismatch assertion from wrong root-path to `assert_not_equal` (redirect goes to
  re-auth, not root); changed `POST /auth/logout` → `POST /oidc/logout` and updated assertion to
  `assert_response :success` (endpoint renders confirmation page per OIDC end session spec when no
  `id_token_hint` is provided).
- Fixed `test/controllers/sign/com/settings/secret_credentials_controller_test.rb`: updated removal
  redirect path assertion from `/settings/secrets/ID` (non-existent route) to
  `/settings/secret_credentials/ID` (actual route generated by
  `RemovalsController#canonical_resource_path`).
- All 213 tests across the 21 modified test files now pass. Pre-existing failures in unmodified
  files (e.g., `IdentityAuthorityInversionGuardTest` — sign routes contain `namespace :oidc`
  requiring config change) remain blocked.

## Batch 1

- Fixed the `sign/app` telephone signup param mismatch so create and OTP submit flows read
  `client_telephone`, matching the form generated by `form_with model: @user_telephone`.
- Removed stale `sample` wording from the `app` telephone signup error summary.
- Verified Ruby syntax for the touched controller and test file.
- Full test execution was blocked locally because `test_com_principal_db` is missing.
